# rules/workflow.md — 开发流程纪律

## 铁律 1：记忆隔离 + 实践入库
- 每位开发者的 AI 本地记忆（`WorkBuddy: .workbuddy/memory/`；`CodeBuddy: .codebuddy/`）**互不干扰、不进 git**（`.gitignore` 已忽略）。
- 验证过的好实践 → 写成 `demo/` 示例，**随仓库提交**，供团队复用。

## 铁律 2：平台现实
- {{PLATFORM_RULE}}

## 铁律 3：代码风格
- 新代码沿用 {{STYLE_RULE}}（见 `frontend.md`）。

## 复杂度预检（每次任务第一步）★

参见 `AGENTS.md`「复杂度分级」表。AI 收到任务后**自动判定 L0-L3 级别**并告知用户。

- **L0 极简**：直接执行，不加载本文件后续流程
- **L1 简单**：跳过任务分类/UI 设计/planning 阶段，直接进入实现
- **L2 常规**：走完整流程（澄清 → 分类 → 实现 → Review → 提交 → 复盘）
- **L3 复杂**：走完整流程 + **规划模式**（澄清 → 分类 → **规划四阶段** → 实现 → Review → 提交 → 复盘）

> 用户可随时纠正 AI 的判定级别。

---

## 铁律 4：先澄清再开发（硬门槛）
接到任何开发任务，**动手前必须向用户确认**：
1. **新增还是修改？**
2. **是什么功能？** （一句话说清业务目标）
3. （若新增）**归属哪个业务域？**

确认后进入**任务分类阶段**（读取 `rules/skill-routing.md`），决定后续激活的 Skill 集。

### 任务分类（硬门槛之后，实现之前）
AI 必须在此阶段判定任务类型并激活对应 Skill：
- **UI 密集任务**（新页面/改版/设计稿还原）→ 激活 UI Skill 调度（见 `skill-routing.md`）
- **逻辑密集任务**（状态管理/Hooks/工具函数）→ 仅激活框架 Skill
- **Bug 修复 / API 对接** → 不激活额外 Skill

分类结果必须**告知用户**，获得确认后再进入下一阶段。

### 复杂任务规划（L3 任务强制）★

当任务被判定为 L3 复杂级别时，**跳过 UI/UX 设计阶段**，直接进入 `rules/planning.md` 所描述的四阶段规划：

```
理解需求（Socratic 提问）
  → 方案设计（2-3 方案对比 + 分段展示）
    → 任务拆解（bite-sized 2-5 min tasks）
      → 自审（placeholder 扫描 + 一致性检查）
        → 用户终审
          → 写入 specs/<slug>/
            → 开始实现
```

**硬门槛**：方案被用户确认前，**不得写任何实现代码**。

> L3 任务在规划阶段产出 `specs/<slug>/` 四件套。实现阶段直接按 `tasks.md` 中的清单逐项执行。

### UI/UX 设计（UI 任务强制）
UI 密集任务在进入实现前，必须走 UI 设计阶段：
- 有设计稿 → 激活 `impeccable` skill（如已安装）
- 无设计稿 → AI 给出布局/交互建议，用户确认后进入实现
- 设计结果必须先确认，**不得跳过确认直接实现**

### 落点决策树
<!-- AI-INSERT: 根据项目实际情况（有无主包/分包/_inject等概念）定制此表 -->
{{LANDING_DECISION_TREE}}

## 大型 / 高风险任务 → 走规划模式 或 specs

**L3 复杂任务** → 走 `rules/planning.md` 四阶段规划流程，产物写入 `specs/<slug>/`。

**L2 常规但较大的任务**（满足任一即开 `specs/` 四件套：`proposal` / `design` / `tasks` / `acceptance`）：
<!-- AI-INSERT: 项目特定的大/高风险判据 -->
{{HIGH_RISK_CRITERIA}}

## 任务闭环：从实现到交付

### 阶段 A：实现
1. 按 `frontend.md` 风格写代码
2. 按框架规则（`react.md` 或 `vue.md`）写框架特定代码
3. 框架 Skill（Vercel 官方 react-best-practices / Vue 官方 vue-best-practices，如已安装）实时约束
4. 每完成一个 task，自测通过后再勾选（见 `testing.md`）

### 阶段 B：Review（提交前必检）
1. 对照 `rules/review.md` 五轴清单逐项自查
2. 若有 UI 变更 → 激活 web-design-guidelines（系统化审计）或 taste-skill（审美审查），如已安装
3. 走 specs 的任务 → 补填 `acceptance.md` 验证记录
4. 不走 specs 的普通任务 → PR 描述中贴出验证命令与结果

### 阶段 C：提交
- **Commit 格式**：`type(scope): description`（遵循 [Conventional Commits](https://www.conventionalcommits.org/)）
  - type：`feat` / `fix` / `refactor` / `style` / `chore`
  - scope：模块名或文件（如 `flight`、`order`、`home`）
  - description：中文描述，动词开头
- **PR 描述**至少包含：做了什么、验证方式、受影响平台/模块。
- 走 specs 的任务 → PR 必须链接 spec 目录路径。

### 阶段 D：交付后复盘
任务合入主干后，AI **应主动问一次**：
> "合入了——过程中有没有值得记录的坑？要不要记到 `rules/` 或做成 skill？"（详见 `sop-init-frontend` skill 的「盲点留存」机制）

有发现的坑 → 追加到对应 `rules/xxx.md` 的「已知陷阱」节。
可复用好写法 → 写一个 `demo/` 示例。

---

## 任务闭环速查
| 阶段 | 适用级别 | 动作 | 产出 |
|------|---------|------|------|
| 复杂度预检 ★ | L0-L3 | 判定级别并告知用户 | 级别标签（L0/L1/L2/L3） |
| 澄清 | L1-L3 | 确认新增/修改 + 功能 + 落点 | 任务描述 |
| 任务分类 | L2-L3 | 读 `skill-routing.md` → 激活 Skill | Skill 激活计划 |
| 规划 ★ | L3 | 四阶段：理解→设计→拆解→自审（`planning.md`） | `specs/<slug>/` 四件套 |
| UI 设计 | L2（UI 任务） | interface-design / ui-ux-pro-max | 布局方案 / UX 模式 |
| A 实现 | L1-L3 | 写代码（frontend.md + 框架 rules + 框架 Skill） | 可运行的功能 |
| B Review | L1-L3 | 五轴检查 + UI 审查 + 填验证记录 | `review.md` 全部打勾 / spec 的 `acceptance` 填完 |
| C 提交 | L1-L3 | conventional commit + PR 证据 | 干净的 git history |
| D 复盘 | L1-L3 | 问一句"有坑吗" | 更新 `rules/` / `demo/` 或 skill |
