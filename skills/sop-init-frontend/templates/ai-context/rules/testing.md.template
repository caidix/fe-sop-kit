# rules/testing.md — 验证基线

<!-- AI-INSERT: 根据项目是否有单测，选择下面一种模式：
  有单测 → 列出测试命令 + 必须通过的覆盖率阈值
  无单测 → 以 类型检查 + 构建 作为验证基线 -->

---

## TDD 工作流（推荐用于 L3 复杂任务）★

灵感来自 Superpowers `test-driven-development`。L3 复杂任务**强烈建议**采用 RED-GREEN-REFACTOR 循环。

### RED-GREEN-REFACTOR 循环

```
RED      写一个会失败的测试（验证测试本身有效）
  ↓
GREEN    写刚好够让测试通过的最小代码
  ↓
REFACTOR 在测试全部通过的前提下重构代码
  ↓
COMMIT   提交（一个 task = 一个 commit）
  ↓
（回到 RED，下一个 task）
```

### 适用场景

| 场景 | 是否必须 TDD | 说明 |
|------|------------|------|
| L3 复杂任务 | **强烈建议** | 规划模式产出的每个 task，用 TDD 循环落地 |
| L2 常规 + 有单测框架 | 建议 | 新逻辑先写测试 |
| L2 常规 + 无单测框架 | 可选 | 至少补类型检查 + 手动验证 |
| L1 简单 / L0 极简 | 不要求 | 直接写代码 + 验证即可 |
| Bug 修复 | **建议先写复现测试** | 确认 bug 存在 → 修复 → 测试通过 → 永不回退 |

---

## 必过关卡
<!-- AI-INSERT: 列出每次提交前必须通过的检查，每行一个 -->
{{MANDATORY_CHECKS}}

## 验证证据（强制）
- 走 `specs/` 的任务：把命令与关键输出写进 `specs/<slug>/acceptance.md` 的「验证记录」。
- 普通任务：在 PR 描述里贴出验证结果。
- 未附证据视为未完成。

## 自测清单
<!-- AI-INSERT: 根据项目补充检查项 -->
- [ ] {{CHECK_1}}
- [ ] {{CHECK_2}}
- [ ] {{CHECK_3}}
- [ ] {{CHECK_4}}
- [ ] {{CHECK_5}}
- [ ] 交互反馈到位，错误有兜底

## 禁止
- 禁止跳过类型检查直接提交。
- 禁止在受影响平台/环境上不验证就声称完成。
- 禁止改动影响其他模块却不确认。

---

## 验证前置（Verification Before Completion）★

灵感来自 Superpowers `verification-before-completion`。

**在说「做完了」之前，必须自己验证过：**

1. **改动真的生效了** — 不是 "理论上应该没问题"，是实际跑过/看到结果
2. **没引入新问题** — `tsc` 无新错误、构建成功、lint 无新增警告
3. **受影响平台都跑过** — 见 `rules/platform.md` 的部署矩阵，每个目标编译产物都验证过
4. **边界条件处理了** — 空数据、异常网络、极端输入 → 不崩、有兜底

**反例（禁止）：**
- "代码写完了，应该可以" — 没验证过，不算完成
- "本地跑过了，构建没试" — 必须构建验证
- "H5 测过，小程序应该一样" — 多端项目必须每端验证
