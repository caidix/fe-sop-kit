# rules/planning.md — 复杂任务规划模式

> 本文件是 **L3 复杂任务** 的强制执行流程。触发后，AI 必须按四阶段走完，**规划确认前不得写任何实现代码**。
> 灵感来源：ECC `planner.md`（结构化方案）+ Superpowers `brainstorming`（苏格拉底式提问）+ Superpowers `writing-plans`（bite-sized 任务粒度）。

---

## 触发条件（满足任一即进入规划模式）

| 触发条件 | 说明 |
|---------|------|
| **复杂度判定为 L3** | AI 在 AGENTS.md 入口处判定为 L3 的任务（新功能、架构变更、重构、跨模块改动） |
| **用户明确要求** | 用户说「这是复杂需求」「先帮我梳理」「需要规划」「帮我设计方案」「出个计划」 |
| **高风险信号** | 涉及鉴权/支付/数据迁移/破坏性变更 |

---

## 硬门槛（HARD GATE）

**规划确认前，禁止以下行为：**
- ❌ 写任何实现代码
- ❌ 创建组件文件
- ❌ 修改现有代码
- ❌ 安装依赖

**允许的行为：**
- ✅ 读取项目文件（理解现状）
- ✅ 搜索代码库（确认影响范围）
- ✅ 提出问题（澄清需求）
- ✅ 展示方案（纯文本/图表）
- ✅ 写入 specs/ 目录（记录方案）

---

## 四阶段流程

```
用户提出复杂需求
  │
  ├─→ 阶段 1：理解需求（Socratic Questioning）
  │     每次只问一个问题
  │     聚焦：Why / Who / What / Constraints / Success
  │
  ├─→ 阶段 2：方案设计（Design）
  │     提出 2-3 种方案 + 对比 + 推荐
  │     分段展示设计，每段等用户确认
  │
  ├─→ 阶段 3：任务拆解（Plan）
  │     每个任务 2-5 分钟可完成
  │     精确文件路径 + 代码骨架 + 验证方式
  │     → 写入 specs/<slug>/
  │
  ├─→ 阶段 4：自审（Self-Review）
  │     placeholder 扫描 / 一致性检查 / 范围检查
  │     → 修复后请用户终审
  │
  └─→ 实现（Handoff）
        用户批准后，按 specs/ 中的 task 清单逐个实现
```

---

## 阶段 1：理解需求（苏格拉底式提问）

**规则：每次只问一个问题，等用户回答后再问下一个。**

不要一次堆砌 5 个问题。收敛到你对需求的理解足够清晰为止——通常 3-6 个问题即可。

### 必问维度（按优先级）

1. **Why** — 这个功能解决什么问题？谁会用？
2. **What** — 用户期望的具体行为是什么？输入 → 输出？
3. **Scope** — 范围有多大？涉及哪些页面/模块？
4. **Constraints** — 有什么限制？已有设计稿？时间线？技术栈约束？
5. **Success** — 怎么做完了算"成功"？验证标准是什么？

### 提问技巧

- 问选择题而非开放题（"A 方案还是 B 方案？"优于"你想怎么做？"）
- 发现矛盾时指出："你前面说 X，但这里又提到 Y，这两者是怎么配合的？"
- 如果需求涉及多个独立子系统，**立即标记**：先拆成大块，逐个规划（每个子系统单独走一轮规划四阶段）

### 阶段 1 出口条件

```
你确信自己能回答以下问题后才进入阶段 2：
- 这个需求要做什么（一句话）
- 影响哪些文件/模块
- 用户期望的核心行为
- 有什么特别的约束或风险
```

---

## 阶段 2：方案设计（Design）

### 2.1 提出 2-3 种方案

对每种方案说明：
- 核心思路（2-3 句）
- 涉及的文件与改动量
- 优点 / 缺点
- 复杂度与风险估计

**以你推荐的方案为首**，说明推荐理由。

### 2.2 分段展示设计

按复杂度分段展示，每段**等用户确认**后继续下一段：

| 段落 | 内容 | 何时可跳过 |
|------|------|-----------|
| **架构** | 数据流、组件树、模块交互 | 无新增模块时可简化为 2-3 句 |
| **关键文件** | 每个文件的改动类型（新增/修改/删除）+ 职责说明 | 永远不跳过 |
| **数据 / 接口** | API 请求/响应、状态结构、Props 接口 | 纯 UI 改动可跳过 |
| **错误处理** | 空态、加载态、错误态、边界条件 | 永远不跳过 |
| **测试策略** | 单元测什么、集成测什么、E2E 测什么 | 永远不跳过 |

### 2.3 设计原则

- **YAGNI 无情** — 从每个方案中删除不必要的功能
- **隔离与清晰** — 每个模块有一个明确职责、通过定义好的接口通信
- **遵循现状** — 新代码沿袭项目现有模式，不引入新范式
- **可独立交付** — 方案应可分批 merge，每批独立有价值

### 阶段 2 出口条件

```
用户对所有设计段落说了「可以」或「继续」，且没有遗留的待确认项。
```

---

## 阶段 3：任务拆解（Plan）

将确认后的方案拆成 bite-sized 任务，写入 `specs/<slug>/`。

### 3.1 任务粒度标准

```
一个任务 = 2-5 分钟可完成（含测试）
- 写一个函数 → 1 个任务
- 写这个函数的测试 → 1 个任务
- 在组件中集成 → 1 个任务
- 不要 "实现用户登录功能"（太大）→ 拆成 5-8 个微任务
```

### 3.2 任务格式

每个任务必须包含：

```markdown
### Task N: [任务名]

**文件：**
- 新增：`exact/path/to/file.tsx`
- 修改：`exact/path/to/existing.ts:123-145`

**接口：**
- 输入：函数签名 / Props 类型 / API 参数
- 输出：返回值 / 渲染结果 / 事件

- [ ] **Step 1: [动作描述]**
  ```typescript
  // 具体的代码结构（不是伪代码，是可直接写出来的骨架）
  ```

- [ ] **Step 2: 验证**
  运行：`tsc --noEmit` 或 `npm run build`
  期望：无类型错误
```

### 3.3 禁止的写法（Placeholder 红线）

以下写法视为**任务拆分失败**，不得出现：
- ❌ "TBD" / "TODO" / "待定" / "后续补充"
- ❌ "适当的错误处理" / "添加必要的验证" / "处理边界情况"
- ❌ "参考 Task N"（不写具体内容）
- ❌ 描述做什么却不给代码骨架的步骤
- ❌ 引用了未在任何任务中定义的类型/函数/方法

### 3.4 存储

方案写入以下文件（模板见 `specs/TEMPLATE.md`）：

```
specs/<YYYY-MM-DD-slug>/
├── proposal.md      # 阶段 1+2 产物：背景、范围、方案
├── design.md        # 阶段 2 产物：架构、数据流、关键文件
├── tasks.md         # 阶段 3 产物：任务清单（bite-sized）
└── acceptance.md    # 阶段 4 + 实现后填写
```

---

## 阶段 4：自审（Self-Review）

写好 spec 文件后，用新眼光审视一遍：

### 自检查清单

1. **Placeholder 扫描**：有没有 TBD/TODO/待定/模糊表述？→ 修正
2. **内部一致性**：`design.md` 和 `tasks.md` 有没有矛盾？→ 修正
3. **范围检查**：有没有超出 proposal 定义的范围？→ 要么剪掉，要么扩大 proposal
4. **歧义检查**：有没有任何需求可以被解释为两种不同含义？→ 明确指定一种
5. **类型一致性**：`tasks.md` 中的函数签名是否与 `design.md` 中定义的一致？→ 修正

### 阶段 4 出口条件

```
自审通过后，请用户 review specs/ 文件：
"方案已写入 specs/<slug>/，请审核。确认后我开始按 task 清单实现。"
→ 用户确认后，进入实现阶段。
```

---

## 实现阶段（Handoff）

用户批准方案后，按 `specs/<slug>/tasks.md` 中的清单逐个执行：

1. **每完成一个 task** → 勾选 `- [x]`
2. **每完成一个 task** → 跑对应的验证命令
3. **每完成 3-5 个 task** → 做一次 git commit
4. **全部 task 完成** → 走 `rules/review.md` 五轴 Review
5. **Review 通过** → 填 `specs/<slug>/acceptance.md` 验收记录 → 交付

---

## 快速参考：何时走完整规划 vs 简化 spec

| 场景 | 走完整规划（planning.md） | 简化 spec（仅 specs/） |
|------|----------------------|---------------------|
| 新模块/新功能（3+ 文件） | ✅ | — |
| 架构变更 / 重构 | ✅ | — |
| "帮我梳理一下这个需求" | ✅ | — |
| 涉及支付/鉴权/数据迁移 | ✅ | — |
| 2 个文件以内的小功能 | — | ✅（跳过阶段 1/2，直接写 proposal + tasks） |
| UI 微调 / Bug 修复 | — | ❌ 连 spec 都不用 |

---

## 规划模式速查

| 角色 | 行为 |
|------|------|
| 你是 AI | 提问、设计、拆任务、自审——但**不写实现代码**直到批准 |
| 用户 | 回答、确认、纠正——**每个阶段结束必须明确说「继续」** |
| 产物 | `specs/<slug>/` 下的四件套（可追溯、可 review） |
