# rules/review.md — 代码审查清单（提交前必检）

> 代码写完 + 自测通过后，提交 PR 前按此清单逐项自查。
> 此文件同时作为 AI 辅助 Review 的 prompt：审查时应覆盖所有轴，不只盯代码风格。

## 五轴审查（每轴至少一条）

### 1. 正确性
- [ ] 实现是否吻合 spec / 需求描述（对照 `specs/<slug>/proposal.md` 或任务描述）？
- [ ] 边界条件：空数据、超长文本、网络失败、并发操作是否处理？
- [ ] 新增/修改的逻辑是否被条件守卫正确圈定（不影响其他模块/航司/端）？
- [ ] 数据流：状态更新是否正确触发 UI 刷新？是否有竞态（setState 在异步回调后）？

### 2. 可读性
- [ ] 命名是否自解释（变量、函数、组件名）？
- [ ] 文件结构与风格是否与 `rules/frontend.md` 一致？
- [ ] 复杂逻辑是否有注释说明意图？
- [ ] 是否有死代码或注释掉的代码残留？

### 3. 架构
- [ ] 新增组件/模块是否放在正确的层级（主包 / 分包 / 公共组件 / 页面级组件）？
- [ ] 抽象是否合理：没有过度封装一个只用一次的 hook，也没有把可复用逻辑硬塞进组件？
- [ ] 新增依赖（npm 包）是否有必要？是否用内部已有能力替代？
- [ ] 路由/页面注册点是否正确（手写路由 / 模板注入 / file-based 对应位置）？

### 4. 安全
- [ ] 用户输入是否经过校验/转义（防 XSS）？
- [ ] 敏感信息（token、手机号、身份证）是否明文出现在 URL / 日志 / localStorage？
- [ ] 操作是否需要登录鉴权？是否做了登录态检查？
- [ ] 跳转/重定向 URL 是否校验了来源？

### 5. 性能
- [ ] 是否有不必要的重复渲染（`useMemo` / `React.memo` 缺失）？
- [ ] 列表是否用了虚拟滚动或分页（长列表 > 50 条）？
- [ ] 图片是否做了懒加载 / 尺寸优化？
- [ ] 是否有未清理的定时器 / 事件监听（useEffect 缺少 cleanup）？

## 可执行检查（验证优于说教）★

> 移植自 harness-engineering：与其口头说"不要这样写"，不如放一个会报错的检测命令。
> 提交前至少跑一遍本节命令；任一非零退出即不可提交。命令按项目实际工具调整。

| 轴 | 命令（示例，按项目实际调整） |
|----|----------------------------|
| 类型/正确性 | `npx tsc --noEmit` |
| 构建 | `{{BUILD_COMMAND}}`（见 project-map.md，如 `npm run build`） |
| 风格/死代码 | `grep -rn "console\.\(log\|debug\)" src/` 应无输出；`npx eslint src/ --max-warnings 0` |
| 安全 | `grep -rnE "dangerouslySetInnerHTML|eval\(" src/` 应无意外命中 |
| 性能 | 跑一次构建，检查产物体积是否超出 `frontend.md` 性能预算 |
| SOP 健康度 | `node scripts/sop-check.mjs --strict`（占位符已填 / 版本一致 / 无 TODO 残留） |

> `sop-check.mjs --strict` 在 CI 中作为硬门禁：未填占位符 / 缺必要 rules / 版本漂移均会阻断。

## 前端专项检查

### 多端/响应式
- [ ] 受影响平台是否全部验证（见 `platform.md`）？
- [ ] 平台特定代码是否通过后缀文件或守卫正确隔离？

### 可访问性
- [ ] 交互元素是否有可访问的 label / role？
- [ ] 颜色对比度是否可读？是否仅靠颜色传达信息？
- [ ] 键盘导航是否可用（Tab 顺序、焦点管理）？

### UI 状态完整度
- [ ] **加载态**：请求中是否有 loading 反馈？
- [ ] **空态**：无数据时是否有空态提示？
- [ ] **错误态**：接口失败是否有兜底提示？是否有重试入口？
- [ ] **边界态**：极端数据（超长、0、负数）是否不崩？

## 提交前证据链
- [ ] 自测已过：`tsc --noEmit` + 构建命令结果已贴到 PR 描述
- [ ] 走 specs 的任务：`acceptance.md` 的验证记录已填完
- [ ] 多端/多航司验证已标注（仅需声明覆盖范围，不强制全端截图）
