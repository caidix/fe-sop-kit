# demo — 好实践示例（随仓库提交）

本目录沉淀**验证过的正确写法**，供团队复用与新代码参照。

## 约定
- 每个子目录是一个独立示例（页面 / 组件 / hook），遵循 `ai-context/rules/frontend.md` 的风格规范。
- **仅作参考**：`demo/` 位于 `src/` 之外，不参与项目构建，也不被类型检查纳入。
- 新增示例：新建 `demo/<name>/`，并在此文件登记。

## 现有示例
| 目录 | 说明 |
|------|------|
<!-- AI-INSERT: 随项目发展追加 -->
