# spec: <slug>

状态：草稿 / 已确认 / 实现中 / 已交付
创建日期：YYYY-MM-DD

---

## 1. proposal.md（范围）

### 背景与目标
<!-- AI-INSERT: 为什么要做？解决什么用户问题？成功标准是什么？ -->

### 类型
- [ ] 新增  [ ] 修改
- 落点：<!-- 主包 / 分包 <module> / _inject / 手写路由 / file-based -->
- 影响平台：<!-- h5 / weapp / tt / web / 其他 -->
- 影响范围：<!-- 列出受影响的模块或页面 -->

### 不在范围内
<!-- AI-INSERT: 明确不做什么（不是模糊的"以后再做"，是本次 spec 明确排除的内容） -->

---

## 2. design.md（方案）

### 关键文件
| 文件 | 改动类型 | 说明 |
|------|----------|------|
<!-- AI-INSERT: 列出每个受影响文件，标注新增/修改/删除 -->

### 数据流 / 接口
<!-- AI-INSERT: 数据从哪来到哪去？涉及哪些 API？请求/响应结构？ -->

### 多端 / 多环境处理
<!-- AI-INSERT: h5/weapp/tt 等是否有差异逻辑？环境变量如何区分？ -->

### 风险与缓解
<!-- AI-INSERT: 技术风险（性能/兼容/回退）+ 缓解措施 -->

---

## 3. tasks.md（任务拆分）

<!-- 每项必须可验证，不可验证的任务不算任务 -->
- [ ] T1 <!-- 描述 + 验证方式 -->
- [ ] T2

---

## 4. acceptance.md（验收）

### 验证记录
| 命令 | 结果 | 时间 |
|------|------|------|
<!-- AI-INSERT: tsc、build、lint 等命令 -->

### 五轴 Review 自检（对照 rules/review.md）
- [ ] 正确性：吻合 spec，边界已处理
- [ ] 可读性：风格一致，命名自解释
- [ ] 架构：层级正确，无过度抽象
- [ ] 安全：输入已校验，鉴权到位
- [ ] 性能：无多余渲染，资源已优化

### 多端 / 多环境验证
| 平台/环境 | 构建结果 | 行为确认 |
|-----------|----------|----------|
<!-- AI-INSERT: 每个编译目标平台的验证结果 -->

### 风险回顾
<!-- AI-INSERT: spec 中预见的风险是否发生？有无新风险？ -->

### 复盘（合入后填）
<!-- AI-INSERT: 值得记成 rule 的坑？可复用写法？ -->

### 截图 / 演示（按需）
<!-- AI-INSERT: 如有 UI 变更，附截图/GIF；如纯逻辑变更，删除此节 -->

---

## 自审清单（写完后、实施前执行）★

写好 spec 后、动手写代码前，用新眼光过一遍：

### 1. Placeholder 扫描
- [ ] 没有 TBD / TODO / 待定 / 后续补充 / 模糊表述
- [ ] 所有 <!-- AI-INSERT --> 区域已填充为具体内容

### 2. 内部一致性
- [ ] `design.md` 中的架构与 `tasks.md` 中的具体实现一致
- [ ] 函数名、Props 类型、API 路径在三份文件间无矛盾
- [ ] `proposal.md` 的「影响范围」覆盖了所有 `design.md` 中列出的文件

### 3. 范围检查
- [ ] 没有超出 `proposal.md`「不在范围内」的内容
- [ ] 如果写的过程中发现需要扩大范围 → 回头更新 proposal

### 4. 歧义检查
- [ ] 没有任何需求可以被解释为两种不同含义
- [ ] 模糊词（"适当的""必要的""合理的"）已替换为具体标准

### 5. 任务可验证性
- [ ] `tasks.md` 中每个 task 都有明确的验证方式
- [ ] 没有 "实现 XXX 功能" 这种不可验证的描述
- [ ] 每个 task 包含了具体的文件路径

> 自审发现的问题**就地修正**，不需要重跑自审。修正完后告知用户：「spec 自审通过，请 review。」
